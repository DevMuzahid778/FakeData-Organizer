import React from 'react';
import { useLoaderData } from 'react-router-dom';

const Home = () => {
    const usersData = useLoaderData()
    return (
        <div>
            
        </div>
    );
};

export default Home;